# Бот-помощник 112 — городской сервис обращений @OdinOdinDwa112bot

Бот создан для хакатона **«Моя профессия — IT»**.  
Цель — быстрый и удобный приём обращений жителей по вопросам ЖКХ, благоустройства и мелких происшествий с последующей обработкой администраторами в мессенджере.

---

## 🚀 Стек

- **Python 3.10+**
- **python-telegram-bot v20+** (асинхронный Telegram Bot API)
- **SQLite + aiosqlite** (асинхронное хранилище)
- **python-dotenv** (загрузка переменных окружения)
- **asyncio** (асинхронная логика)
- Локальные модули: `main.py`, `db.py`, `config.py`, `utils.py`, `init_db.py`

Авторы: **Cinnapik (Станислав)** и **Danoff (Даниил)**

---

## ✨ Основной функционал (соответствие ТЗ)

### Пользователь
- Обращения принимаются **только в личном чате** с ботом.
- Можно отправить **текст**, **фото/видео/документ** (с подписью) и **геолокацию**.
- После отправки бот отвечает: _«Заявка принята, номер `T...`»_.
- Раздел **«Мои обращения»** показывает ваши заявки и их **статусы**.

### Администратор
- Ввод прав администратора через `/admin <секретный_код>`.
- Уведомления о новых заявках (включая текст, вложения и координаты).
- Просмотр **последних** и **активных** заявок, **открытие по тикету**.
- Смена статуса: **«В обработке»**, **«Завершено»**, **«Отклонено»**.  
  ▶ Финальные статусы нельзя менять повторно.
- **Диалог с пользователем**: оператор может начать/завершить диалог по тикету, сообщения идут в **личные чаты** (PM).
- **Разовый ответ** пользователю по конкретной заявке.
- **Подменю «Сервис/Отчёты»**:
  - **Экспорт** отчёта (`csv`/`txt`) за период.
  - **Массовая рассылка** с предпросмотром и подтверждением.
  - **Отчётность** (сколько всего, завершено, отклонено).
  - **Опасные операции**: массовое удаление/закрытие/чистка по дате.

### Команды (для администраторов)
- `/admin <секрет>` — получить права администратора.
- `/export csv 2025-11-01 2025-11-10` — экспорт за период (также доступно из меню).
- `/cleanup active|all|before YYYY-MM-DD` — очистка заявок.
- `/bulkclose` — массово закрыть **активные** заявки.
- `/broadcast <текст>` — массовая рассылка с предпросмотром.

> Бот дополнительно предоставляет кнопки и инлайн-управление внутри **админ-меню** и подменю **«Сервис/Отчёты»**.

---

## 📁 Структура проекта

```
.
├─ main.py         # Точка входа, хэндлеры команд, меню, диалоги, коллбэки
├─ db.py           # Работа с БД (users/requests/replies, отчёты/чистка)
├─ config.py       # Конфигурация: пути, загрузка env
├─ utils.py        # Утилиты (генерация тикетов и др.)
├─ init_db.py      # Инициализация БД (вызвается при старте из main.py)
├─ requirements.txt
├─ .env            # Секреты/настройки (локально)
└─ bot.db          # SQLite база (создаётся автоматически)
```

---

## 🔧 Подготовка окружения

1) Установите **Python 3.10+**  
2) Склонируйте репозиторий:
```bash
git clone https://github.com/<your-org-or-user>/<repo>.git
cd <repo>
```

3) Создайте и активируйте виртуальное окружение:

**Windows PowerShell**
```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
```

**macOS / Linux (bash/zsh)**
```bash
python3 -m venv .venv
source .venv/bin/activate
```

4) Установите зависимости:
```bash
pip install -r requirements.txt
```

5) Создайте файл **.env** (в корне проекта):
```dotenv
BOT_TOKEN=1234567890:AA...your_bot_token ...
ADMIN_SECRET=your_admin_secret_here
DB_PATH=bot.db
FILES_DIR=files
```

- `BOT_TOKEN` — токен бота от **@BotFather**
- `ADMIN_SECRET` — любой секретный код, который админ введёт командой `/admin <секрет>`
- `DB_PATH` — путь к SQLite базе (по умолчанию `bot.db` в корне)
- `FILES_DIR` — папка для отчётов (будет создана при старте)

> **Важно:** Не коммитьте `.env` в публичный репозиторий.

---

## ▶️ Запуск бота (локально)

```bash
python main.py
```

В логах будет:
```
INFO:telegram.ext.Application:Application started
INFO:bot:DB ready. Files dir: files
```

Откройте Telegram, найдите вашего бота и напишите `/start`.  
Чтобы стать админом, выполните `/admin <ваш_секрет_из_.env>`.

---

## 🧪 Быстро проверить функционал

1. От лица обычного пользователя (любой Telegram-аккаунт):
   - Нажмите **«Создать обращение»**, отправьте текст, опционально медиа и геолокацию.
   - Получите номер тикета и проверьте **«Мои обращения»**.

2. От лица администратора:
   - `/admin <секрет>` → откроется **«Админ-меню»**.
   - **«Последние заявки (5)»** или **«Активные заявки»** → кнопка **«Открыть»**.
   - Запустите **диалог** с пользователем или сделайте **разовый ответ**.
   - Измените **статус** (финальные менять нельзя повторно).
   - Зайдите в **«Сервис/Отчёты»**:
     - **Экспорт отчёта** — задайте формат и период.
     - **Массовая рассылка** — введите текст, подтвердите отправку.
     - **Отчётность** — сводка по статусам.
     - **Удаление заявок (ОПАСНО)** — аккуратно!

---

## 🗄️ Обновления БД и миграции

Схема создаётся автоматически при старте (`init_db`).  
Если вы меняете структуру таблиц вручную и видите ошибки вида _«no such column …»_,  
проще всего **удалить `bot.db`** (если данные не важны) и перезапустить бота — таблицы будут созданы заново.

---

## 🔐 Замечания по безопасности

- Никогда не публикуйте `BOT_TOKEN` и `ADMIN_SECRET`.
- Для продакшена используйте отдельный хост и системный сервис (например, `systemd`) для перезапуска.
- Регулярно выгружайте и чистите старые заявки (подменю «Сервис/Отчёты» → «Удаление заявок»).

---

## 🛠️ Деплой (вариант без Docker)

1) Скопируйте репозиторий на сервер.
2) Создайте виртуальное окружение, установите зависимости (см. выше).
3) Создайте `.env` с боевыми значениями.
4) Запустите бота: `python main.py`.  
   Для автозапуска настройте `systemd`-unit или используйте `pm2`/`supervisor`.

Пример `systemd`-юнита (Linux):
```ini
[Unit]
Description=bot112
After=network.target

[Service]
User=ubuntu
WorkingDirectory=/opt/bot112
Environment="PYTHONUNBUFFERED=1"
ExecStart=/opt/bot112/.venv/bin/python /opt/bot112/main.py
Restart=always

[Install]
WantedBy=multi-user.target
```

---

## 🤝 Авторы

- **Cinnapik (Станислав)**
- **Danoff (Даниил)**


---

Удачной эксплуатации! Если что-то сломалось — смотрите логи и не стесняйтесь задавать вопросы 🙂
